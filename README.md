# Backend-project

Web application made with express. I integrated user authentifiation by using the passport.js and epxress-session libraries.For the app to work properly, you'll need a postgresql database running locally and an .env with database credentials. To run the app you need to have the following libraries installed : ejs,express,passport,express-session,pg and bcrypt.
